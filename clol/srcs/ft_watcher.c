#include "minishell.h"
#include "ft_socket.h"
void	ft_watcher(t_connection connection)
{
	while (42)
	{
		if (ft_socket_to_read())
		{
		    signal(, sig_t func);
		}
		printf("server >> %.200s\n", toto);
	}
}
